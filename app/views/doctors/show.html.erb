<%= form_for @message do |form| %>
  <div class="form-group">
    <p>
      <%= form.label :body, "Message" %>
      <%= form.text_area :body, class: "form-control" %>
    </p>

<p><%= form.button 'Submit Message', class: "btn btn-success" %></p>
<% end %>
  
